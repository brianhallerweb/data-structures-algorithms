//Selection Sort uses a nested for loop to swap the first index with the smallest
//element, the second index with the second smallest element... etc.

function selectionSort(arr) {}

module.exports = selectionSort;
